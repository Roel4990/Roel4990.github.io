import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "리스크 판단 로직 정리 — 발표 자료",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
